<!DOCTYPE html>
<html>

<head>
    <title>CrudSample</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body>
    <div class="header-container" style="display: flex; justify-content: space-between; align-items: center; padding: 10px 20px; background-color: #f8f8f8; border-bottom: 1px solid #eee;">
        <%= link_to "Rails App", root_path, class: "app-title-link" %>

        <div class="auth-links">
            <% if flash[:notice] %>
            <p style="color: green"><%= flash[:notice] %></p>
            <% end %>
            <% if flash[:alert] %>
            <p style="color: red"><%= flash[:alert] %></p>
            <% end %>

            <% if current_user %>
            <span>환영합니다, <%= current_user.username %>!</span>
            <%= button_to "로그아웃", logout_path, method: :delete, class: "form-submit-button" %>
            <% else %>
            <%= link_to "회원가입", signup_path %> |
            <%= link_to "로그인", login_path %>
            <% end %>
        </div>
    </div>

    <%= yield %>
</body>

</html>